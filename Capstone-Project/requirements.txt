# ═══════════════════════════════════════════════════════
# Capstone Assignment 2 — Requirements
# Selenium Python Automation Framework
# Default Browser: Firefox (Mozilla) — Arch Linux
# ═══════════════════════════════════════════════════════

# ── Core Selenium ─────────────────────────────────────
selenium>=4.18.1

# ── WebDriver Manager (auto-downloads geckodriver) ────
# Automatically downloads the correct geckodriver for Firefox
webdriver-manager>=4.0.1

# ── Testing Frameworks ────────────────────────────────
pytest>=8.0.0
pytest-html>=4.1.1

# ── HTML Test Runner (for Unittest HTML reports) ──────
html-testRunner>=1.2.1

# ── Utilities ─────────────────────────────────────────
configparser>=5.3.0

# ─────────────────────────────────────────────────────
# Arch Linux Setup Notes:
#   Firefox is installed by default on most Arch setups.
#   If not: sudo pacman -S firefox
#   geckodriver is handled automatically by webdriver-manager.
#   Optional (system geckodriver): sudo pacman -S geckodriver
# ─────────────────────────────────────────────────────

# ── Optional: Parallel Testing ────────────────────────
# pytest-xdist>=3.5.0

# ── Optional: Allure Report ───────────────────────────
# allure-pytest>=2.13.2
